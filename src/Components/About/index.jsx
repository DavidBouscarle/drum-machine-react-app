import styled from "styled-components";

const Index = () => {
    return (
        <Wrapper>
            <h2>About us</h2>
        </Wrapper>
    );
};

const Wrapper = styled.div``;

export default Index;
